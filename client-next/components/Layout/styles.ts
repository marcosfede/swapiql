import css from 'styled-jsx/css'

export default css`
  .root {
    min-height: 100vh;
    width: 100vw;
    background-color: var(--main-bg-color);
    color: var(--main-fg-color);
    font-family: 'Lato';
    margin: 0 auto;
  }
`
